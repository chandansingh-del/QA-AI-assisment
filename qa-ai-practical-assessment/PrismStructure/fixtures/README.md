# Custom Playwright fixtures

Place `test.extend()` definitions here:

- `authenticatedPage` — logged-in customer session
- Page object fixtures (`loginPage`, `cartPage`, etc.)
- API client fixture with bearer token

Do not implement tests in this folder.
