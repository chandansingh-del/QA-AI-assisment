# Utilities

Pure helper functions with **no** `@playwright/test` imports.

Examples to implement:

- `env.js` — read environment variables with defaults
- `dataGenerator.js` — unique email/username builders
- `dateHelper.js` — valid DOB for registration (18–75 years)
